# calculator/models.py

from django.db import models

# No models are needed for now if we are just serving static HTML files.
# This file can be expanded later if you need to store calculator-related data.
